#include<stdio.h>
#include<conio.h>
main()
{ 
   int x,z,y;
   printf("Este programa calculao maior numero entre os tres");
   printf("Digite o primeiro numero:");
    scanf("%d", &x);
   printf("Digite o psegundo numero:");
    scanf("%d", &z); 
   printf("Digite o terceiro numero:");
    scanf("%d", &y);  
    
    if ((x>z) && (x>y) && (z<>y))
   
    if((x>z) && (x>y))
       printf("o maior numero entre os tres:", x);
     
    else
      if((z>x) && (z>y))
       printf(" o maior numero entre os tres:", z);
       
    else
     if((y>x) &&(y>z))
      printf("o maior numero entre os tres:" ,y);
      
getch();
return 0:
}                
